package com.example.aman_singh_myruns2

/*
class DataEntryViewModel(application: Application) : AndroidViewModel(application) {

    private val db = AppDatabase.getDatabase(application)
    private val exerciseEntryDao = db.exerciseEntryDao()

    fun saveExerciseEntry(exerciseEntry: ExerciseEntry) {
        viewModelScope.launch(Dispatchers.IO) {
            exerciseEntryDao.insertEntry(exerciseEntry)

            // Notify the UI
            viewModelScope.launch(Dispatchers.Main) {
                Toast.makeText(getApplication(), "Entry saved!", Toast.LENGTH_SHORT).show()
            }
        }
    }
}


 */
